// Math = função da própria linguagem

const raio = 5.6
const area = Math.PI * Math.pow(raio, 2) // Math.PI = número de PI -- Math.pow = potencia

console.log(typeof Math) // Math é um objeto
console.log(area) // pode usar um .toFixed()
